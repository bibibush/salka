# Expo/Metro 는 pnpm 의 격리(symlink) node_modules 레이아웃에서 모듈 해석이 불안정하다.
# Expo 공식 monorepo 가이드에 따라 hoisted 링커를 사용해 node_modules 를 평탄화한다.
# (web/api 워크스페이스는 hoisted 여부와 무관하게 동작하며, 워크스페이스 패키지는 여전히 symlink 로 연결된다.)
node-linker=hoisted

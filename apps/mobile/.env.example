# 백엔드 API 베이스 URL. Expo 는 EXPO_PUBLIC_ 접두사 변수만 클라이언트에 노출한다.
# iOS 시뮬레이터는 localhost, Android 에뮬레이터는 10.0.2.2 를 사용한다.
EXPO_PUBLIC_API_BASE_URL=http://localhost:8000

# Expo 액세스 토큰 (EAS build/submit/update 인증에 사용). GitHub Actions 에서는
# Secrets.EXPO_TOKEN 으로 참조된다. 실제 값으로 교체할 것.
EXPO_TOKEN=<expo-access-token>

# [CI 전용 / 로컬 미사용] mobile-cd.yml 의 main push 배포 활성화 플래그.
# GitHub Actions Variable `MOBILE_CD_ENABLED` 를 'true' 로 두면 main push 시
# EAS 배포가 실제로 동작한다(미설정이면 push 는 안전하게 no-op). eas init(projectId)
# 와 EXPO_TOKEN·스토어 자격증명 준비가 끝난 뒤에만 켠다.
# MOBILE_CD_ENABLED=<true-when-ready>
